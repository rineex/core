import type { Linter } from 'eslint';

/**
 * Default ESLint flat config array
 * This is the fully configured ESLint config ready to use
 */
declare const eslintConfig: Linter.Config[];
export default eslintConfig;
